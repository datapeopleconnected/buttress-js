{
  "name": "pt1-1",
  "selection": {
    "role": {
      "@eq": "test1"
    }
  },
  "config": [{
    "endpoints": ["GET"],
    "query": [{
      "schema": ["car"],
      "access": "%FULL_ACCESS%"
    }]
  }],
  "projection": [{
    "schema": ["car"],
    "keys": ["name"]
  }]
}