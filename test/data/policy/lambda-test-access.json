{
	"name": "lambda-test-access",
	"version": "1",
	"selection": {
		"lambda": {
			"@eq": "TEST_ACCESS"
		}
	},
	"config": [
		{
			"verbs": ["GET", "SEARCH", "PUT", "POST", "DELETE"],
			"schema": ["%ALL%"],
			"query": {
				"access": "%FULL_ACCESS%"
			}
		}
	]
}
