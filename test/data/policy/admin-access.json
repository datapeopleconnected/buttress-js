{
  "name": "admin-access",
  "selection": {
    "adminAccess": {
      "@eq": true
    }
  },
  "config": [{
    "verbs": ["GET", "SEARCH", "PUT", "POST", "DELETE"],
    "schema": ["%ALL%"],
    "query": {
      "access": "%FULL_ACCESS%"
    }
  }]
}